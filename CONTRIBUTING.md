# Contributing to MarketLens

## Branching Model
- `feature/*` -> `develop` -> `main`

## Commit Convention
We use Conventional Commits (feat, fix, chore, docs, ci).

## PR Process
1. Create a feature branch.
2. Ensure all tests and linting pass.
3. PR must be approved and CI must pass before merge.
